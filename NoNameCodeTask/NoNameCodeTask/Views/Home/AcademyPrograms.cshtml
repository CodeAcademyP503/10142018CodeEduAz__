@model IEnumerable<NoNameCodeTask.Models.SubjectPrograms>

@{
    Layout = null;
}

<section id="services"
         class="">
    <div class="heading">
        <h1>TƏDRİS PROQRAMLARIMIZ</h1>
    </div>
    <div class="wrapper">
        <div class="classes flexbox between flex-wrap">
            @foreach (var item in Model)
            {
                <a class="class flexbox middle web" style=" background-image:url(@item.IconPath); background-color:@item.ClickPath" href="#">@item.Name</a>
            }

           
        </div>
    </div>
</section>